<div class="card mb-5 mx-auto my-2" style="width: 18rem;">
  <img src="/public/img/{{imagen}}" class="card-img-top"  alt="{{nombre}}">
  <div class="card-body">
    <h5 class="card-title">{{nombre}}</h5>
    <p class="card-text">{{descripcion}}</p>
    <p class="card-text">${{precio}}</p>
    <a href="#" class="btn btn-primary">Agregar carrito</a>
    <a href="#" class="btn btn-info">Detalle</a>
  </div>
</div>